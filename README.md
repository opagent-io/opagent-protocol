# OpAgent Protocol
OpAgent Protocol is a distributed agent operation and interoperability standard that extends the MCP specification so agents can invoke one another like modular plugins or web services.

OpAgent Protocol 是一个分布式智能体互操作协议，在扩展的 MCP 规范之上构建，使各类智能体能够像插件或 Web 服务一样互相调用并共享能力。